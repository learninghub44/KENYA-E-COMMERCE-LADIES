# Seller App Surface

Placeholder for the future Next.js App Router seller workspace.

The current repository does not include a root `app/` or `apps/web/` application scaffold, so Agent 03
implemented the seller domain as framework-agnostic services and documented the intended API/page
surface. When the web scaffold lands, seller pages should consume `lib/seller` and `lib/kyc` instead
of duplicating onboarding or KYC logic.
