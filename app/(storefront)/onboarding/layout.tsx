import type { ReactNode } from "react"

export const metadata = {
  title: "Onboarding | Zuri Market",
  description: "Set up your Zuri Market account",
}

export default function OnboardingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
