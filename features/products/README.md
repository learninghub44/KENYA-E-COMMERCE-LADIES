# features/products

Owned by Agent 04 (Marketplace Engineer).

The repository has no `app/api` route-handler scaffold yet (same gap Agent 03 noted). The
framework-agnostic product domain logic lives in `lib/products/` and is ready to be wired into
Next.js route handlers under `app/api/v1/products`, `app/api/v1/...` once that scaffold exists.

See `docs/products/` for architecture, lifecycle, variants, and API reference, and
`docs/handoffs/agent-04.md` for the full handoff.
