# Reports Feature

Reports are handled by `lib/reports`. Buyers, sellers, and support surfaces may create reports;
only actors with `admin.moderate` may search, assign, note, dismiss, or resolve them.

Supported targets are products, sellers, buyers, messages, and stores.
